---
import Layout from "../layouts/Layout.astro";
import { StudyView } from "@/components/study/StudyView";
---

<Layout title="Sesja nauki">
  <main
    id="study-main"
    class="mx-auto flex min-h-[80vh] w-full max-w-5xl flex-col gap-10 px-4 py-12 lg:py-16"
    aria-labelledby="study-heading"
  >
    <section class="text-center">
      <p class="text-xs font-semibold uppercase tracking-[0.3em] text-primary/80">Powtórka zaplanowanych fiszek</p>
      <h1 id="study-heading" class="mt-3 text-3xl font-bold tracking-tight text-foreground md:text-4xl">
        Przeprowadź sesję nauki SM-2
      </h1>
      <p class="mt-4 text-base text-muted-foreground md:text-lg">
        Odkrywaj odpowiedzi, oceniaj swoją znajomość materiału, a my zajmiemy się wyznaczeniem kolejnych powtórek.
      </p>
    </section>
    <StudyView client:load />
  </main>
</Layout>
